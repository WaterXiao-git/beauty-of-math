# MathViz 项目约定（AGENTS）

## 改动存档（强制约定）

**每次代码改动后，必须在 `docs/CHANGELOG.md` 顶部追加一条记录**，再向用户汇报。

记录格式（最新在上，按时间倒序）：

```
## YYYY-MM-DD · 改动标题
**模块**：...
**内容**：...
**涉及文件**：...
**验证**：...
```

## 项目背景

- 「数韵之美」智能交互式可视化平台（数学学习/课堂演示）
- 按需求文档《数韵之美功能效果与开发需求说明》分阶段改造
- 架构目标：规则优先路由 + 双模型协作（参考 docs/ 下架构说明）
- 当前进行中：规则优先路由核心 → 前端接入 → 双模型分支 → 生成与临时实验